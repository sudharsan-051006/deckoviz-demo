# deckoviz_web_f

## Content (blogs)

This project uses a simple markdown-based CMS for blog posts. Add posts by creating a markdown file in `src/content/blogs` with frontmatter fields described in `src/content/blogs/README.md`.

Important: frontmatter `id` must be unique. Tags are discovered automatically.